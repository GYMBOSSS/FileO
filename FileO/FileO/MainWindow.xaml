<Window x:Class="FileO.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileO"
        mc:Ignorable="d"
        Title="MainWindow" Height="534" Width="942" Background="{x:Null}">
    <Window.Resources>

    </Window.Resources>
    <Border CornerRadius="20" Background="#FF93FFFD" Margin="10,0,10,10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="329"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button x:Name="MyButton" Template="{DynamicResource BTTemplate}" Margin="10,10,219,10" 
                    BorderBrush="#FF009CFF" BorderThickness="2" Background="#FFE5FFB9" FontSize="20" FontFamily="Arial">
                Options
            </Button>
            <TreeView x:Name="FileTree" Grid.Row="1" Background="#FF93FFFD" Margin="10,0,2,10" ></TreeView>
            <ComboBox x:Name="Drives" Grid.Row="0" Style="{DynamicResource StylishComboBox}" 
                     BorderBrush="#FF009CFF" BorderThickness="2" Background="#FFE5FFB9"
                     FontSize="20" FontFamily="Arial" Margin="126,10,10,10"></ComboBox>
        </Grid>
    </Border>
</Window>
